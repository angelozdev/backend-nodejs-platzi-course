version: '3'
name: 'backend-express'
services:
  api:
    container_name: api
    image: angelozdev/api:latest
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules
